import { PrdCardDirective } from './prd-card.directive';

describe('PrdCardDirective', () => {
  it('should create an instance', () => {
    const directive = new PrdCardDirective();
    expect(directive).toBeTruthy();
  });
});
